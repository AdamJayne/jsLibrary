// spaces can be significant, though not very often
var white = "space";
// this can also be re written without unnecessary spaces
var white="space";
// notice that there is still a space between var and white
// spaces are used to keep some words from combining into an expression
// that javascript cannot read.

// some words are reserved by the JS language in order to perform actions


	//These reserved words are:
	abstract boolean break byte case catch char class const
	continue debugger default delete do double else enum export
	extends false final finally float for function goto if 
	implements import in instanceof int interface long native
	new null package private protected public return short
	static super switch synchronized this throw throws transient
	true try typeof var volatile void while with

//